{
  "name": "jrod-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --log-level warn --write ."
  },
  "devDependencies": {
    "prettier": "3.2.5",
    "vite": "^5.2.0"
  }
}
